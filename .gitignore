node_modules/
dist/
.DS_Store

# All dotenv variants (.env, .env.production, .env.development, .env.local,
# .env.production.local, etc.) — issue #92: only the *.local patterns were
# covered, so a plain .env or .env.production would not have been ignored.
.env
.env.*
!.env.example

.worktrees/

# Local tool/session files — not part of the project
.superpowers/
.claude/scheduled_tasks.lock

# Supabase CLI local link metadata (project ref, pooler URL/host, versions).
# Not secrets, but unnecessary recon — keep it out of the public repo (MED-6).
supabase/.temp/
